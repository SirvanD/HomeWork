while (greet !== "BYE") {
    var greet = prompt ("speak to grandma");
    if (greet.toUpperCase() !== greet){
        alert ("HUH WHAT DID YOU SAY");
    } else if (greet === "BYE") {
        alert("GOODBYE")
    } else {
        alert("NO, NOT SINCE 1938");
    }
}